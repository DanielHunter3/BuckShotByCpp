#include "Mode.h"
// #include "Item.h"
// #include "Details.h"

int main() { 
    // Items items;
    // items.generate(8);
    // std::cout << items.ReturnObjects().size() << '\n';
    // for (auto &i: items.ObjectsByStr()) std::cout << i << '\n';
    // std::cout << "------------------------------------------------\n";
    // if (in_vector(items.ReturnObjects(), BEER))
    //     std::cout << "YEAR\n-----------------------------------------------";
    //     items.erase(BEER);
    // for (auto &i: items.ObjectsByStr()) std:: cout << i << '\n';
    mains();
    system("pause");
}