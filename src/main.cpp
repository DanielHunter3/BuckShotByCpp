#include "Mode.h"
#include <iostream>
#include "Details.h"

int main() { 
    //mains();

    bool x = 0;
    if (!x) std::cout << "false is true" << std::endl;

    // for (int i = 0; i < 10; ++i) 
    //     std::cout << randint() << std::endl;
}